<%*
  let title = tp.file.title
  if (title.startsWith("Untitled")) {
    title = await tp.system.prompt("Title");
    await tp.file.rename(title);
  } 
    
tR += `---
Locations: ""
Departure: 
Return:
Country:
Travel Type:
Travel Status: Planned
Review Status: 
tags: Interest/Travel/Trip
---
[[🌍 Lean Travel]]
# <%* tp.file.title %>
`;
%>
# Overview

> [!multi-column]
> > ## Outbound
> > -
> > 
> > 
> > ## Inbound
> > -

```dataviewjs
// Access the current file's frontmatter and name
const frontmatter = dv.current().file.frontmatter;
const fileName = dv.current().file.name;

// Check if 'locations' exists in the frontmatter
if(frontmatter && frontmatter.locations) {
	//Spilt the 'locations' exists in the frontmatter
	const [centerLat, centerLng] = frontmatter.locations.toString().split(",").map(coord => coord.trim());
	//Construct the mapview block as a string
	const mapviewBlock = `\`\`\`mapview
{
	"name": "Default",
	"mapZoom": 6,
	"centerLat": ${centerLat},
	"centerLng": ${centerLng},
	"query": "linkedfrom:\\"${fileName}\\" OR linkedto:\\"${fileName}\\" ",
	"chosenMapSource": 0,
	"showLinks": false,
	"linkColor": "red",
	"markerLabels": "left"
}
\`\`\``;

	// Display the mapview block
	const container = document.createElement("div");
	container.innerHTML = mapviewBlock;
	dv.container.appendChild(container);
} else {
	dv.paragraph("❌ Error: `Locations` 欄位不存在或為空（格式應為：`緯度,經度`）。");
}
```
# Transport
## Outbound
### Train

### Plane

### Car

## Inbound
### Train

### Plane

### Car

> [!multi-column]
> > [!NOTE]
> > # Accommodation
> 
> > ```button
> > name 🏠 Add Accommodation
> > type command
> > action QucikAdd: New Accommodation
> > ```
> > ^button-btnAccommodationAdd

> [!multi-column]
> > [!NOTE]
> > # Landmarks
> 
> > ```button
> > name 🏠 Add Landmark
> > type command
> > action QucikAdd: New Landmark
> > ```
> > ^button-btnLandmarkAdd

> [!multi-column]
> > [!NOTE]
> > # Food & Beverages
> 
> > ```button
> > name 🏠 Add other Location
> > type command
> > action QucikAdd: New Location
> > ```
> > ^button-btnLocationAdd

# Activities

# Destination-specific considerations
| Item            | Comment |
| --------------- | ------- |
| Currency        |         |
| Driving         |         |
| Power           |         |
| Religion        |         |
| Vaccinations    |         |
| Visa            |         |
| Water qualities |         |
# Short Review
```dataviewjs
const currentPage = dv.current();
const departureDate = dv.date(currentPage.Departure);
const returnDate = dv.date(currentPage.Return);
const daysBetween = returnDate.diff(departureDate, 'days').days;

dv.paragraph(`Trip duration: ${daysBetween} days`);

```
Rating::
Summary::

# Long Review